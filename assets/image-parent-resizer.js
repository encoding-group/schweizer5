const resize = function() {
    const images = document.querySelectorAll(".img");
    console.log(images)

    images.forEach((image) => {
        console.log(image)
    })
}

export default {
    resize
}